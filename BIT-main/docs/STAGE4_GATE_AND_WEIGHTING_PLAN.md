# 阶段4：不确定性门控与加权实施方案

日期：2026-08-22

## 1. 阶段目标

阶段4把阶段2冻结的掩码失败概率与阶段3冻结的位姿失败概率转化为关键帧
“接受、降权、拒绝”决策，减少不可靠观测进入形状更新闭环。实施顺序是先影子
评估、再离线回放、最后在线接入；影子模式只记录决策，不改变BIT输出。

## 2. 概率语义

冻结模型输出均为失败概率：

```text
p_mask_fail = P(mask IoU < 0.90)
p_pose_fail = P(pose fails 5deg/5cm)
q_mask = 1 - p_mask_fail
q_pose = 1 - p_pose_fail
q_base = q_mask * q_pose
```

阶段4 v1禁止直接把两个“失败概率”相乘后当作可靠度。`q_base`是两个可靠度
相乘，等价于在条件独立近似下要求掩码与位姿同时可靠。

## 3. 分阶段优先级

### P0：影子门控基线

改动位置：

- `scripts/build_stage4_shadow_gate.py`
- `tests/test_stage4_shadow_gate.py`

输入使用阶段2/3严格嵌套LOSO的冻结OOF概率，同一对象/序列的三个seed保持同折，
避免在训练数据上报告门控效果。首轮诊断策略为：

```text
q_base >= 0.80       -> accept, weight=1
0.30 <= q_base < .80 -> downweight, weight=q_base/0.80
q_base < 0.30        -> reject, weight=0
```


该阈值仅用于验证管线和方向，不作为冻结上线阈值。首轮83帧结果显示其没有接受
任何关键帧，安全帧误拒率53.8%，因此被明确判定为过度保守的负向控制。风险
排序本身有效：unsafe risk AUROC为0.854，危险帧非接受覆盖率为100%。

随后使用`select_stage4_gate_thresholds_loso.py`在不查看外层对象/序列的情况下
选择阈值。内层约束为安全帧拒绝率不超过10%、安全帧非接受率不超过50%，
目标为最大化危险帧拒绝召回并兼顾降权覆盖。该结果用于判断是否存在可行工作点，
仍需独立序列验证后才能冻结在线阈值。
每个关键帧输出当前mask/pose失败标签、未来5帧位姿失败、未来10帧内模型更新事件，
以及更新前后各3帧失败率。主要验证指标为：

- unsafe risk AUROC；
- reject unsafe recall与reject safe rate；
- accept/downweight/reject各组的当前和未来失败率；
- 被门控覆盖的退化模型更新数量。

### P1：可见性与信息量消融

首版记录但不启用：

```text
q_visibility = geometric_mean(
    visible_boundary_ratio,
    effective_contour_ratio,
    1 - occlusion_ratio)
q_information = clip(min_view_angle_deg / 20deg, 0, 1)
```

原因是阶段3已证明可见性特征在`toy_plane/02`上能改善排序，但全局融合会恶化
校准。只有严格LOSO消融证明其降低错误更新且不提高安全帧拒绝率后，才允许进入
主门控。

P1已完成12种消融。`q_mask*q_pose`保持最高的确认性AUROC 0.854和3.8%的
安全误拒率；绝对可见性/信息量未稳定改善两个问题序列。因果相对信息量点估计
达到0.864，但bootstrap增益区间跨0，属于探索候选。当前不冻结阈值；进入P2
前需使用独立新增关键帧确认。详见`STAGE4_P1_GATE_ABLATION_REPORT.md`。

独立确认及预注册扩样现已完成：36/36次运行完整，得到95个预热后对齐关键帧。
因果相对信息量的总体AUROC为0.745，高于固定`q_mask*q_pose`基线的0.713，且
总体安全误拒率/安全非接受率为0%/30%；但它在`duplo_dude/01`和
`toy_plane/02`上没有改变基线决策，未满足“至少一个问题序列严格改善”和三seed
方向一致要求。因此`enter_p2=false`，P2继续阻塞，转入P1-v2特征修复。详见
`STAGE4_INDEPENDENT_CONFIRMATION_REPORT.md`。

### P2：离线回放与阈值冻结

在序列级嵌套交叉验证中比较：

1. `q_mask`单独门控；
2. `q_pose`单独门控；
3. `q_mask*q_pose`；
4. 加入`q_visibility/q_information`；
5. hard reject与soft weighting。

阈值选择目标为：安全关键帧拒绝率受控时最大化错误更新召回。阈值只能在内层
训练序列选择，外层序列只评估。完成后冻结阈值、数据哈希和策略版本。

### P3：在线影子记录

在Python/C++通信帧号对齐的基础上，在线计算冻结模型概率并记录：

```text
frame_index,p_mask_fail,p_pose_fail,q_base,
q_visibility,q_information,decision,weight,decision_time_ms
```

该步骤仍不改变关键帧发送、轮廓优化和模型发布。

### P4：在线加权和信赖域

仅在P0-P3通过后启用：

- reject：不进入形状训练缓冲；
- downweight：轮廓/掩码损失乘以shadow weight；
- accept：原权重；
- 低`q_pose`时缩小位姿优化信赖域，并禁止产生模型更新候选。

在线策略必须提供总开关和影子模式开关，以便与原BIT逐帧配对比较。

## 4. 验收标准

- 错误模型更新次数下降至少50%；
- 困难序列ADD-AUC提升至少3点；
- 5deg/5cm成功率不下降；
- 普通序列ADD-AUC退化不超过1点；
- 在线门控额外开销低于5%；
- 三个seed方向一致，并报告序列bootstrap 95%置信区间。

## 5. 当前边界

P0结果只能回答“冻结风险分数能否筛出危险关键帧”，不能直接证明跟踪精度已经
提升。只有在线门控与基线进行同序列、同seed配对实验后，才能报告ADD-AUC和
5deg/5cm的阶段4性能增益。

独立确认已经证明因果相对信息量具有总体排序信号，但没有证明它能稳定修复两个
问题序列。不得根据本次确认标签放宽阈值后直接进入P2；任何P1-v2公式都必须重新
冻结，并在未查看的新seed混合标签关键帧上确认。

P1-v2开发与公式冻结现已完成。冻结的“直接`q_pose`因果风险增幅”在131帧严格
序列LOSO中将危险保护率从60.9%提高到83.9%，安全误拒/安全非接受率为
4.5%/45.5%，两个问题序列的危险保护均达到100%。该结果仍属于开发证据；P2
继续阻塞，下一步严格使用冻结阈值在未查看的seed 53–58上独立再确认。详见
`STAGE4_P1V2_DEVELOPMENT_REPORT.md`与`config/stage4_p1v2_reconfirmation.json`。


P1-v2 冻结公式已在 seed 53–58 的独立再确认中通过进入 P2 的条件。P2 首轮现已
完成 6 个采集运行、8 个候选事件的观测暴露与精确几何反事实回放。风险门控把
本轮危险保护率从 50% 提高到 100%，但安全非接受率升至 57.1%；原软权重、
0.50 下限及硬门控均未稳定改善候选 IoU loss。因此 P2 阈值尚未冻结，P3 继续
阻塞。下一优先级是用梯度对齐或近似 leave-one-out influence 建模观测对验证
几何的边际效用，再用独立 seed 62–64 确认。详见
`STAGE4_P2_OFFLINE_REPLAY_REPORT.md`。


观测边际效用 v1 已实现并完成 seed 62–64 独立验证。风险先验非负梯度匹配在
seed 59–61 开发集上改善 5/7 个候选，但独立集只改善 2/6，平均 IoU loss
变化为 +0.0000847，时序标准差也轻微上升；三个种子方向不一致，最终
`pass=false`。该方法不冻结为在线策略，P3 继续阻塞。下一优先级改为短时域
近似 leave-one-out，并使用全新 seed 65–67 独立验证。详见
`STAGE4_P2_INFLUENCE_REPORT.md`。

短时域全量/删一边际效用 v1 已按预注册协议完成。seed 59–64 开发集仅改善
5/13 个可优化事件，三个种子方向退化；冻结 25 步和 softmax 公式后，seed
65–67 独立集的门控安全约束全部通过，但平均 IoU loss 变化为 +0.000785，
只有 3/6 个事件改善，三个独立 seed 的均值均退化。位姿不一致性与时序标准差
虽有改善，仍未通过几何主目标、改善事件比例和 seed 一致性验收。因此该方法
不冻结为在线权重，P3 继续阻塞；后续转向带真实验证损失单调验收的模型更新
信赖域，而不是继续在相同数据上调整权重温度。详见
`STAGE4_P2_SHORT_LOO_REPORT.md`。

LOO 风险预筛选与真实验证损失单调信赖域 v1 已实现并完成 seed 59–64 开发回放。
15 个事件中提交8个、回滚7个，所有最终模型相对同次稳定模型的 IoU 单调性违规
为0；但相对原始候选平均 IoU loss 增加0.001217，仅改善5/15个事件，时序标准差
也出现退化。该结果证明 rollback 安全层有效，但 utility>0 的硬预筛仍会丢失有用
观测。因此 v1 标记为开发否决，不进入在线路径也不启动新独立集。下一优先级是
在同次回放中比较稳定模型、正常全量候选和 LOO 候选的双候选信赖域。详见
`STAGE4_P2_TRUST_REGION_REPORT.md`。

双候选信赖域 v1 已完成 seed 59–64 同次精确回放。稳定/full/LOO 的选择次数为
4/7/4；相对同次 full 和 stable 均无退化，平均 IoU loss 改善0.000646，bootstrap
95% CI 为[-0.001211,-0.000202]。但严格改善仅8/15，seed 62/63持平，且时序
标准差平均增加0.000699，因此原多指标验收仍为 `pass=false`。当前不冻结在线
版本、不消耗新独立 seed；下一优先级是在 full 始终可行的前提下加入位姿与时序
非退化约束，再在可行候选中选择最低 IoU。详见
`STAGE4_P2_DUAL_TRUST_REGION_REPORT.md`。

多指标双候选信赖域已按预注册标准完成 seed 59–64 重选。IoU、位姿和时序逐事件
退化均为0，平均 IoU loss 改善0.000450，bootstrap区间上界小于0；但严格改善
仅4/15，严格改善seed仅3/6，未达到50%和4/6要求。stable只有2/15可行，LOO只有
3/9可行，表明候选优化目标尚未主动产生多指标可行几何。当前不冻结，seed 68–70
保持未使用。下一优先级是在候选优化阶段加入可微位姿扰动一致性和跨验证帧稳定性
正则，而不是放宽已失败的零退化约束。详见
`STAGE4_P2_MULTIMETRIC_TRUST_REGION_REPORT.md`。

候选多指标可行域正则已完成两轮预注册25步筛选。训练帧绝对正则未改善可行率；
相对full的控制帧hinge把LOO可行事件从4/9提高到7/9。冻结多指标选择后，平均
IoU/位姿/时序分别改善0.000677/0.000401/0.000723，bootstrap IoU区间完全低于
0，严格改善seed由3/6提高到5/6；但严格改善事件仅6/15，仍低于50%验收线。
因此不运行200步和seed 68–70。下一优先级是受约束梯度投影或增广拉格朗日，
提高可行候选的IoU边际收益。详见
`STAGE4_P2_CANDIDATE_REGULARIZATION_REPORT.md`。

受约束梯度投影已实现并完成25步预注册筛选。直接以训练IoU为主梯度时只有
3/225步发生投影，严格改善仍为6/15；改为控制IoU对齐后，LOO可行候选达到8/9，
多指标选择为stable/full/LOO=1/6/8，严格改善9/15且6/6个seed均改善。平均
IoU/位姿/时序分别改善0.002494/0.000740/0.001992，bootstrap IoU区间为
[-0.003888,-0.001211]，225步均满足一阶非退化约束。当前状态为筛选通过、
等待seed 59--64的200步精确确认；seed 68--70仍未使用。详见
`STAGE4_P2_CONSTRAINED_PROJECTION_REPORT.md`。

seed 59--64的200步、逐步刷新扰动精确确认已经通过，随后按冻结流水线打开并
完成seed 68--70独立验证。开发确认中10/15个事件、6/6个seed严格改善；独立
验证中7/7个事件、3/3个seed严格改善，平均IoU loss改善0.005384，bootstrap
95% CI为[-0.008072,-0.002944]，位姿与时序均零退化，所有800个独立优化步
满足投影约束。P2离线教师状态冻结为`independent_validation_pass`。当前不直接
进入P4在线执行：下一优先级是把约24.3秒/事件的离线教师蒸馏为额外开销低于5%
的学生策略，再进入P3在线影子记录。seed 68--70不得再用于调参。详见
`STAGE4_P2_PROJECTION_CONFIRMATION_REPORT.md`与
`config/stage4_p2_control_iou_projection_frozen.json`。
